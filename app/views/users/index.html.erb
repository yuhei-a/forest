

<% @user.each do |users| %>

 <%= attachment_image_tag(users, :profile_image, style:"height: 80px; width: 80px;", fallback: "no_image.jpg") %>
 <%= users.name %>
 <%= users.gender %>
 <%= users.prefectures %>
 <%= users.bloodtype %>
 <%= users.sign %>
 <%= users.introduction %>
 <%= link_to "ユーザー詳細", user_path(users) %>

 <% end %>