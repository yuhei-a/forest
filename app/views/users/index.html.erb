

<% @user.each do |users| %>

 <%= attachment_image_tag(users, :profile_image, size:"30×30", fallback: "no_image.jpg") %>
 <%= users.name %>
 <%= users.gender %>
 <%= users.prefectures %>
 <%= users.bloodtype %>
 <%= users.sign %>
 <%= users.introduction %>

 <% end %>