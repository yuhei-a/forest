<body class="main-background">

<%= render "layouts/sidebar" %>

<div class="col-md-6">
  <h2>本当に退会しますか？</h2>
  <div>
    <p>退会する場合は、『退会する』をクリックしてください。</p>
  </div>
  <div>
    <p><%= link_to '退会しない', user_path(current_user.id), class: "btn btn-primary" %></p>
    <p><%= link_to "退会する", withdraw_user_path(current_user.id), class: "btn btn-danger", method: :patch %></p>
  </div>
</div>

<%= render "layouts/blog_infomation" %>

</body>