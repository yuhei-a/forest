<%= render "layouts/sidebar" %>

<div class="col-md-6">

<% if @followed != nil %>
  <table class="table border-bottom">
    <thead>
      <tr>
        <th>プロフィール画象</th>
        <th>名前</th>
        <th>リンク</th>
      </tr>
    </thead>
    <% @followed.each do |followed_user| %>
    　<tbody>
      　<tr>
        　<td><%= attachment_image_tag(followed_user, :profile_image, style:"height: 45px; width: 45px;" ) %></td>
          <td><%= followed_user.name %></td>
          <td><%= link_to "詳細", user_path(followed_user) %></td>
        </tr>
      </tbody>
    <% end %>
  </table>
<% else %>
  <p>現在フォローされているユーザーはいません。</p>
  <p>誰かをフォローしてみましょう！あなたに興味を持って返してくれるかもしれません。</p>
<% end %>
</div>

<aside class="col-md-3 blog-sidebar">
 <div class="p-3 mb-3 bg-dark rounded">
    <h4 class="font-italic">サイト内検索</h4>
    <ol class="list-unstyled">
        <li>
          <%= form_tag(search_path, method: :get) do %>
           <%= text_field_tag 'item' %>
           <%= submit_tag '検索' %>
          <% end %>
        </li>
    </ol>
  </div>

  <div class="p-3 bg-dark">
    <h4 class="font-italic">最新の投稿</h4>
    <ol class="list-unstyled mb-0">
     <% @recent_post.each do |post| %>
      <li><%= link_to post.created_at.strftime("%Y-%m-%d"), post_path(post) %></li>
    </ol>
     <% end %>
  </div>
</aside>